/// Kind of audio currently loaded in the shared app player.
///
/// Used by the mini player to know which view model owns the audio.
enum ActiveAudioType {
  radio,
  reciter,
  sermon,
  sharawy,
  download,
}
